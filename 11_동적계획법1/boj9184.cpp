#include <iostream>
#include <vector>

using namespace std;

int w(int a, int b, int c) {
	vector<int> arr;
	arr.assign()


	return val;
}

int main() {
	int a, b, c;
	int result;
	while (true) {
		cin >> a >> b >> c;
		if (a == -1 && b == -1 && c == -1) break;
		result = w(a, b, c);
		cout << "w(" << a << ", " << b << ", " << c << ") = " << result << "\n";
			
	}
}